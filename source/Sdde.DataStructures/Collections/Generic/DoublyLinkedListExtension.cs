namespace Sdde.Collections.Generic;

public static class DoublyLinkedListExtension
{
    public static IEnumerable<IDoublyLinkedList<T>> Contains<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }

    public static IEnumerable<IDoublyLinkedList<T>> Count<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }

    public static IEnumerable<IDoublyLinkedList<T>> ElementAt<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }

    public static IEnumerable<IDoublyLinkedList<T>> Empty<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }

    public static IEnumerable<IDoublyLinkedList<T>> First<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }

    public static IEnumerable<IDoublyLinkedList<T>> Last<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }

    public static IEnumerable<IDoublyLinkedList<T>> Max<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }

    public static IEnumerable<IDoublyLinkedList<T>> Min<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }

    public static IEnumerable<IDoublyLinkedList<T>> Order<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }

    public static IEnumerable<IDoublyLinkedList<T>> OrderBy<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }

    public static IEnumerable<IDoublyLinkedList<T>> OrderByDescending<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }

    public static IEnumerable<IDoublyLinkedList<T>> OrderDescending<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }

    public static IEnumerable<IDoublyLinkedList<T>> Prepend<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }

    public static IEnumerable<IDoublyLinkedList<T>> Range<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }

    public static IEnumerable<IDoublyLinkedList<T>> Reverse<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }

    public static IEnumerable<IDoublyLinkedList<T>> Take<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }

    public static IEnumerable<IDoublyLinkedList<T>> TakeLast<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }

    public static IEnumerable<IDoublyLinkedList<T>> ToArray<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }

    public static IEnumerable<IDoublyLinkedList<T>> ToList<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }

    public static IEnumerable<IDoublyLinkedList<T>> Where<T>(this DoublyLinkedList<T> input)
    {
        yield return input;
    }
}
